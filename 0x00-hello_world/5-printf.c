#include <stdio.h>

/**
 * main - printing the string in printf function
 * Description: using the prinf function to print the statement
 * "with proper grammar, but the outcome is a piece of art"
 *
 * Return: 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
